## Async Browser Automation Class

A reusable Python class demonstrating asynchronous browser automation with Playwright and Object-Oriented Programming (OOP).

### Key Features
- Encapsulates Playwright lifecycle management (`start`, `navigate`, `stop`) inside a clean class structure.
- Utilizes `asyncio` for non-blocking browser interactions.
- Hardcoded headless controls directly in the browser launcher for simple execution.
